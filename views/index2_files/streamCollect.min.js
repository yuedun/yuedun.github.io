var tenant=window.tenant||{};if(tenant.verify&&(tenant.verify==$.md5(window.location.host))){new Cs({aid:tenant.id||'',clt:tenant.tenantType||'pc',pf:tenant.makeDomain||'demo',uid:tenant.userId||'',lc:tenant.loginMode||'',verify:tenant.verify})}